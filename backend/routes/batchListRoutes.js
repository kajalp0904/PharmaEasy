const express = require('express');
const router = express.Router();

// Placeholder - batch list functionality is handled by batchRoutes.js
// This file exists to prevent a missing module error on startup.

module.exports = router;
